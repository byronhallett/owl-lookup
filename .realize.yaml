settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: owl-lookup
  path: .
  commands:
    install:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
